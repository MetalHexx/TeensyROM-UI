<UserControl x:Class="TeensyRom.Ui.Features.Music.PlayToolbar.PlayToolbarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TeensyRom.Ui.Features.Music.PlayToolbar"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Horizontal">
        <Button Command="{Binding PreviousCommand}"  Content="Previous" VerticalAlignment="Top"/>
        <Button Command="{Binding PlayCommand}"  Content="Play" VerticalAlignment="Top"/>
        <Button Command="{Binding NextCommand}"  Content="Next" VerticalAlignment="Top"/>
        <TextBlock Text="{Binding Song.Title, Mode=OneWay}"/>
        <TextBlock Text="{Binding CurrentTime, Mode=OneWay}"/>

    </StackPanel>
</UserControl>
