<UserControl x:Class="TeensyRom.Ui.Features.Music.PlayToolbar.PlayToolbarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TeensyRom.Ui.Features.Music.PlayToolbar"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Horizontal">
        <Button Command="{Binding PreviousCommand}"  Content="Previous" VerticalAlignment="Top"/>
        <Button Command="{Binding TogglePlayCommand}" Visibility="{Binding IsPlaying, Converter={StaticResource InverseBoolToVisibilityConvertor}}"   Content="Play" VerticalAlignment="Top" Margin="10 0 0 0"/>
        <Button Command="{Binding TogglePlayCommand}" Visibility="{Binding IsPlaying, Converter={StaticResource BoolToVisibilityConvertor}}"   Content="Pause" VerticalAlignment="Top" Margin="10 0 0 0"/>        
        <Button Command="{Binding NextCommand}"  Content="Next" VerticalAlignment="Top" Margin="10 0 0 0"/>
        <TextBlock Text="{Binding CurrentTime, Mode=OneWay}" VerticalAlignment="Center" Margin="10 0 0 0"/>
        <TextBlock Text="{Binding Song.Name, Mode=OneWay}" VerticalAlignment="Center" Margin="10 0 0 0"/>        

    </StackPanel>
</UserControl>
