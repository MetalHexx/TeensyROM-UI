<UserControl x:Class="TeensyRom.Ui.Controls.CornerToolbar.CornerToolbarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TeensyRom.Ui.Controls.CornerToolbar"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
             mc:Ignorable="d">

    <Border Style="{StaticResource ToolbarBorder}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <Button 
                Command="{Binding CacheAllCommand}" 
                ToolTip="Downloads all file location information from the TR. &#x0a;This is vital for the best experience."
                Style="{StaticResource StorageIconButton}"
                Margin="5 0 0 0"
                Grid.Column="0">

                <materialDesign:PackIcon Kind="Download" />
            </Button>

            
        </Grid>
    </Border>
</UserControl>
