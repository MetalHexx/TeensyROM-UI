<UserControl x:Class="TeensyRom.Ui.Controls.CacheButton.CacheButtonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TeensyRom.Ui.Controls.CacheButton"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
             mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <Button 
                Command="{Binding CacheAllCommand}" 
                ToolTip="Indexes all file location information from the TR for the selected storage device. &#x0a;Indexing everything is important for randomization and search functionality."
                Style="{StaticResource StorageIconButton}"                
                Grid.Column="0">

            <materialDesign:PackIcon Kind="Download" />
        </Button>


    </Grid>
</UserControl>
