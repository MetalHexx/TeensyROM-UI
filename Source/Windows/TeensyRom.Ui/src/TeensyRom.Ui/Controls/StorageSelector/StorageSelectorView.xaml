<UserControl x:Class="TeensyRom.Ui.Controls.StorageSelector.StorageSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TeensyRom.Ui.Controls.StorageSelector"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Style="{StaticResource ToolbarBorder}">
        <ComboBox 
            ItemsSource="{Binding Storage, Mode=OneWay}"  
            SelectedItem="{Binding SelectedStorage, Mode=TwoWay}" 
            ToolTip="Select the storage device to view files for on the TR."
            HorizontalContentAlignment="Center"
            materialDesign:HintAssist.Hint="OS" Margin="15 0 10 0">
        </ComboBox>
    </Border>
</UserControl>