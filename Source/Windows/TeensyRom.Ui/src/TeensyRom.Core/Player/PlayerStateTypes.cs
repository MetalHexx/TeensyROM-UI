namespace TeensyRom.Core.Player
{
    public enum PlayState
    {
        Playing,
        Paused,
        Stopped
    }
    public enum PlayMode
    {
        CurrentDirectory,
        Random,
        Search
    }

    public enum SidTimer
    {
        SongLength,
        TimerOverride
    }
}
